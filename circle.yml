dependencies:
  override:
    - bash ./get_selenium.sh
  cache_directories:
    - "libs/"
    - "target"
    - "project/target"
    - "project/project/target"
    - "redis-client/target"
    - "shale-lib/target"
    - "shale-service/target"

test:
  post:
    - sbt assembly

general:
  artifacts:
    - service/target/shale.jar
